git init
git status
git add file_name
git commit -m "meesage"
git restore filename"
git remote add <url>
git remote -v
git push origin master

now i am going to pull the changes
git pull origin branch_name

git branch -> gives in which branch are currently in
git branch branch_name -> this creates new branch

git switch branch_name -> switches to another branch {whose name is mentioned}

to merge the branches--->
    git checkout base-branch
    git merge branch-name
